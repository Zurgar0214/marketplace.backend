{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "Marketplace",
            "database": "${workspaceFolder:marketplace.backend}/marketplace.db"
        }
    ]
}